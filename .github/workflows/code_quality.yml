name: Code Quality
run-name: ${{ github.ref }} to ${{ github.head_ref }} branch

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
      - releases/**
    paths:
      - 'src/**'
      - '.github/**'

jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: echo "Run tests"