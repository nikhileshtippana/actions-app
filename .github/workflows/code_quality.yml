name: Code Quality
run-name: ${{ github.ref_name }}:${{ github.head_ref }}->${{ github.base_ref }}

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - main
      - releases/**
    paths:
      - 'src/**'
      - '.github/**'

jobs:
  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: echo "Run tests"